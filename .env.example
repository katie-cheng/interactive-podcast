# Copy to .env.local and paste your real keys (no quotes). Never commit .env.local.
#
# Claude replies (typed or after voice transcription)
ANTHROPIC_API_KEY=
#
# Voice: Whisper transcribes on our server so it works on strict Wi‑Fi (e.g. school).
# Get a key at platform.openai.com — billing is ~$0.006/min of audio.
OPENAI_API_KEY=

