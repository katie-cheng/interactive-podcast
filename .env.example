# Anthropic API key for /api/chat (voice → Claude responses).
# Copy to .env.local and paste your real key (no quotes). Never commit .env.local.
#
# Wrong:  ANTHROPIC_API_KEY="anthropic API key"
# Right:  ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxx
#
ANTHROPIC_API_KEY=
